import { Editor, Frame, Element } from "@craftjs/core";
export const EditorPage = () => {
  return (
    <div className="h-full h-screen">
      {/* <Editor resolver={{}} enabled={false} onRender={RenderNode}>
        <Viewport>
          <Frame>
            <Element
              canvas
              is={Container}
              width="800px"
              height="auto"
              background={{ r: 255, g: 255, b: 255, a: 1 }}
              padding={["40", "40", "40", "40"]}
              custom={{ displayName: "App" }}
            ></Element>
          </Frame>
        </Viewport>
      </Editor> */}
    </div>
  );
};
